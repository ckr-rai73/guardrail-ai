name: Adversarial Test Suite

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install fastapi uvicorn mcp langgraph google-generativeai supabase pytest httpx

    - name: Run adversarial tests
      run: |
        # Run specific adversarial test scripts or pytest here
        # E.g., pytest backend/tests/ or executing specific scripts
        echo "Running adversarial tests..."
        # Add actual execution command based on standard test execution pipeline
        # Example: python backend/adversarial_test_phase102_generation.py
        # For now, we'll placeholder a generic test runner if a pytest suite exists
        
        # We also want to execute the adversarial committee test as a comprehensive check
        python backend/adversarial_committee_test.py
